1) При сохранении заданной структуры (столбцы-операции, строки-детали)
программа способна работать с различными размерами исходных таблиц,
это не проблема. Расширение же таблицы происходит в excel таблице и к
к работе программы отношения не имеет.

2) Не считаю необходимым переназначать названия операций\деталей, т.к 
для работы программы это не критично, но при подобном изменении пропадает
интерпритируемость данных. (Сами не поймем где какая деталь\операция)

3) На любом этапе работы есть возможность как посмотреть текущие изменения\
данные в таблице с помощью data.head(), также вывести в отдельный файл
(например data.to_excel())

4) добавил возможность выгрузить промежуточную таблицу

5) Функционал для выборки детали\деталей уже есть. (выгрузка результатов также добавлена)